import NavBar from "../NavBar/NavBar";
const Rooms = () => {
  return (
    <div>
      <nav>

        <NavBar number_page={3}/>
        <h1>Hello Rooms</h1>
      </nav>
    </div>
  );
};

export default Rooms;
